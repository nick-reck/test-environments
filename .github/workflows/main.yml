on:
  push:
    branches:
      - main
jobs:
  deploy-dev:
    name: campaigns-dev
    uses: ./.github/workflows/deploy.yaml
    permissions:
      actions: write
    with:
      environment: dev
      service: campaigns
    secrets: inherit

  deploy-test:
    needs: deploy-dev
    name: campaigns-test
    uses: ./.github/workflows/deploy.yaml
    permissions:
      actions: write
    with:
      environment: test
      service: campaigns
    secrets: inherit

  deploy-prod:
    needs: deploy-test
    name: campaigns-prod
    uses: ./.github/workflows/deploy.yaml
    permissions:
      actions: write
    with:
      environment: prod
      service: campaigns
    secrets: inherit
